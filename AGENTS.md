# AGENTS.md — Aluka 工程协作指南

> 面向 AI 代码助手与团队协作者的工程规范与操作手册。
> 每日待办与证据落在 `.work/TODO/<YYYYMMDD>/README.md`，详见本文「开发工作流」。

Aluka 是纯 Rust 实现的高性能、现代化 JavaScript / TypeScript 运行时，旨在提供毫秒级冷启动、极低常驻内存开销，并全面兼容 **Node.js 22 (LTS)** 规范生态。

---

## 一、项目目标定义

### 1. 核心定位与愿景
- **现代化运行时**：纯 Rust 实现，专注于毫秒级超快冷启动与极低常驻内存占用；
- **权威规范生态**：全面兼容 **Node.js 22 (LTS v22.23.1+)** 规范行为与 ECMAScript / test262 标准；
- **轻量独立分发**：单二进制交付，零外部运行时依赖，杜绝不必要的 C 库绑定。

### 2. 不可违背的硬性架构原则
1. **权威行为以 Node.js 22 LTS 与官方规范为准**：
   语义分歧以官方 Node.js 22 LTS 与 ECMAScript 标准为唯一权威 oracle，通过严格的自动化差分对拍保障，杜绝主观臆断。
2. **跨组件严格以 ISA 字节码契约解耦**：
   `aluka-bytecode` 是编译前端（`alukac`）与执行后端（`aluvm`）之间的唯一接口。前后端组件保持完全解耦，严禁直接跨层依赖底层内部类型。
3. **元数据为单一事实来源且编译期强制保证**：
   新增与修改指令必须在 `Op::stack_effect` 的穷尽 `match` 中登记，缺少分支直接触发编译失败。
4. **静态内存安全与 `unsafe` 默认全仓禁用**：
   Workspace 级强制 `unsafe_code = "deny"`。极少数底层内存操作必须附带完备的 `// SAFETY:` 注释论证不变量。
5. **单元拆分纪律（禁止 Fat `lib.rs`）**：
   `lib.rs` 仅作为公共 API 门面与声明入口（原则上不超过 100 行），内部业务必须按单一职责原则拆分到具体子模块单元。

### 3. 组件协同与演进路线
1. **当前开发模式（前端 alukac 与后端 aluvm 独立开发）**：
   当前研发期严格以编译前端 `alukac` 与执行后端 `aluvm` 独立组件形式推进。前端负责 JS/TS 语法解析、类型剥离与字节码发射，后端负责字节码校验、解释执行与内置库实现，两者通过强类型 ISA 字节码契约独立演进与解耦验证。
2. **终局合并方案（达成 Node.js 全面兼容后程序合并，流程不变）**：
   在达成 Node.js 22 全面兼容的目标后，考虑将前后端合并为一个统一的运行时可执行程序；合并仅作为外部分发与命令行交互的体验整合，**内部执行流程与架构层次保持绝对不变**（源码 → 字节码生成/静态校验 → VM 解释执行的流水线解耦机制保持不变）。

---

## 二、开发工作流定义

每个工作日及重要任务会话必须严格执行以下工程协作工作流：

### 1. 任务登记流（开工先登记）
动手修改代码前，必须在当日 TODO（`.work/TODO/<YYYYMMDD>/README.md`）登记待办任务、预期目标与验收标准，保持清晰的目标跟踪。

### 2. 代码变更与复审流（复审后交付）
- 所有代码变更在交付前必须逐块 review `git diff`，确保变更与既定目标严格一致，严禁夹带无关改动；
- 文档与代码注释以中文为主，保持公开 API 准确清晰；
- 严禁假定测试通过，严禁提交存在编译警告或格式问题的代码。

### 3. 门禁验证与真实证据闭环流（真实证据闭环）
任务完成后必须运行并通过全套自动化门禁，并在当日 TODO 中回填真实命令输出证据：

```bash
# 1. 代码格式化门禁
cargo fmt --all --check

# 2. 严格 Clippy 静态分析门禁（零警告允许）
cargo clippy --all-targets --all-features -- -D warnings

# 3. 全工作区全量测试门禁（含单元测试、Node.js 22 LTS 差分对拍与黄金用例）
cargo test --workspace --all-features
```
