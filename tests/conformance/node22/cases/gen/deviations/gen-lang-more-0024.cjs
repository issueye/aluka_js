// conformance 生成用例（gen.mjs 产出，勿手改）：lang-more
try { console.log(JSON.stringify((() => { const a = []; for (let i = 0; i < 3; i++) a.unshift(i); return a })())) } catch (e) { console.log("ERR", e.name) }
